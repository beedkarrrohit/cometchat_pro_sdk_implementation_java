<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cometchatprojava">

    <application
        android:name=".Initializer"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CometChatProJava">
        <activity android:name=".chatActivity.ChatScreenActivity" android:theme="@style/Theme.Splash"></activity>
        <activity android:name=".login_activity.LoginActivity" />
        <activity android:name=".cometChatActivity.CometChatActivity" />
        <activity android:name=".CallingScreen.CallingScreen" android:theme="@style/Theme.Splash"/>
        <activity android:name=".SplashActivity" android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>